require 'spec_helper'

describe Omniauth::Timely do
  it 'has a version number' do
    expect(Omniauth::Timely::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
